<%= link_to("<< Back to List", {:action => 'index'}) %>
<div>
	<h2>Update Admin user</h2>

	<%= form_for(:admin_user, :url => {:action => 'update', :id => @admin_user.id}, :html => {:method => :put}) do |f| %>
	<%= render(:partial => "form", :locals => {:f => f}) %>

	<div>
		<%= submit_tag("Update Admin user") %>
	</div>
	<% end %>
</div>